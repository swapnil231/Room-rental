import { ForbiddenemailDirective } from './forbiddenemail.directive';

describe('ForbiddenemailDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenemailDirective();
    expect(directive).toBeTruthy();
  });
});
